退会ページ
<% if @user == current_user %>

	   <%= link_to "退会", user_destroy_public_users_path(@users.id, :user => {:deleted => 'true'}), method: :delete, class:"destroy_#{@users.id}  btn-sm btn-danger", "data-confirm" => "本当に退会しますか？" %>
<% end %>